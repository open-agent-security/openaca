# Gitleaks config for openaca. Test fixtures intentionally contain
# secret-shaped strings (synthetic ot_* tokens, fake credentials in
# redaction tests); they are not real secrets.
[extend]
useDefault = true

[[allowlists]]
paths = ['''tests/''']
