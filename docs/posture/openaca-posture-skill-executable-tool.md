# openaca-posture-skill-executable-tool

Flags skills that declare executable tool access through `allowed-tools`, such
as `Bash` or `Shell`.

Executable tool access can be legitimate, but it expands the skill's capability
surface. Review these skills under normal code-review and change-control.
