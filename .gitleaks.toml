# Gitleaks config for openaca. tests/remote/ contains synthetic ot_*
# tokens used as stand-in credentials in remote-feature tests; not real.
[extend]
useDefault = true

[[allowlists]]
paths = ['''tests/remote/''']
