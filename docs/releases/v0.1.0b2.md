# 0.1.0b2 — dedicated PyPI README + Python 3.11 install guidance

Backfilled retroactively; shipped to PyPI on 2026-05-18.

## Highlights

- **Dedicated PyPI README.** The package's PyPI landing page now uses a project-specific README rather than the repo README, with install + first-run guidance tuned for the page's audience.
- **Python 3.11 install path.** Documentation clarifies the supported interpreter and the `uv tool install`-first install path (no `--pre` / `--prerelease` flags needed once a beta is on PyPI).

## Install

`uv tool install openaca==0.1.0b2` or `pip install openaca==0.1.0b2`.

## Compatibility

Pre-V0 beta — no back-compat hedging.
